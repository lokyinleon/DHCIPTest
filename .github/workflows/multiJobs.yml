name: First Workflow
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  first-job:
    runs-on: ubunta-lastest
    steps:
       - name: Print greeting
         run: echo "Hello"
       - name: Print Name
         run: echo "Name"
         
    
