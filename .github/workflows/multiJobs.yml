name: First Workflow
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    steps:
       - name: Print greeting
         run: echo "Hello"
       - name: Print Name
         run: echo "Name"
         
    
