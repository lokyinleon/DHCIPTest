name: First Workflow
on workflow_dispatch
jobs:
  first-job:
    runs-on: ubunta-lastest
    steps:
       - name: Print greeting
         run: echo "Hello"
       - name: Print Name
         run echo "Name"
         
    
